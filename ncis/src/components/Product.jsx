import React from "react";
import { Box } from "@chakra-ui/react";

import DEVICETEXTS from "../consants/text"
import { Ethical, Capacity, Ocean, Transparency, Information } from '../images/export';

const Product = () => {
    const {name, isLinear, desc} = props; 

    return (
        <Box>

        </Box>
    );
}

